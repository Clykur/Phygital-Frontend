/** Soft emerald — same visual weight as neutral/cancelled chips (border + tint), not solid fill. */
export const STATUS_CHIP_EMERALD =
  "border-emerald-500/30 bg-emerald-500/10 text-emerald-900 dark:text-emerald-100";

/** Due-soon / warning — matched weight to emerald/destructive soft chips. */
export const STATUS_CHIP_AMBER_SOFT =
  "border-amber-500/30 bg-amber-500/10 text-amber-950 dark:text-amber-100";

/** Overdue / cancelled-style alert — soft destructive, not solid red. */
export const STATUS_CHIP_DESTRUCTIVE_SOFT =
  "border-destructive/30 bg-destructive/10 text-destructive";
